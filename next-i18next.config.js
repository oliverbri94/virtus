/** @type {import('next-i18next').UserConfig} */
module.exports = {
  i18n: {
    // Estos son todos los idiomas que soportas en tu sitio.
    locales: ['en', 'es'],
    // Este es el idioma por defecto que se usará si el navegador del usuario
    // no coincide con ninguno de los idiomas soportados.
    defaultLocale: 'en',
  },
};